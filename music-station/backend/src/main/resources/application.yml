server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://dresh.cn:3306/music_station?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

music:
  scan-path: "D:/tempMusic2"
