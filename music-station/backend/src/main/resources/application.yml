server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/music_station?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

jwt:
  secret: "music-station-secret-key"
  expiration-ms: 86400000

music:
  scan-path: "/data/music"
